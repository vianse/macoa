<%= form_for(flow) do |f| %>
  <% if flow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flow.errors.count, "error") %> prohibited this flow from being saved:</h2>

      <ul>
      <% flow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_hash %>
    <%= f.text_field :current_hash %>
  </div>

  <div class="field">
    <%= f.label :data %>
    <%= f.text_field :data %>
  </div>

  <div class="field">
    <%= f.label :prev_hash %>
    <%= f.text_field :prev_hash %>
  </div>

  <div class="field">
    <%= f.label :time %>
    <%= f.text_field :time %>
  </div>

  <div class="field">
    <%= f.label :index %>
    <%= f.text_field :index %>
  </div>

  <div class="field">
    <%= f.label :nonce %>
    <%= f.text_field :nonce %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
